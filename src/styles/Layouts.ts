import { styled } from "styled-components";

export const PageLayout = styled.div`
  max-width: 768px;
  width: 100%;
  margin: 0 auto;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
