import { styled } from "styled-components";

const Logo = () => {
  return <LogoStyle>alocados</LogoStyle>;
};

export default Logo;

const LogoStyle = styled.h1`
  font-size: 1.4rem;
  font-weight: bold;
  font-family: fantasy;
`;
